# Dashboard notes

The custom **HeartSync AI** UI has been reverted.

Use the standard Streamlit entrypoint instead:

```powershell
streamlit run app.py
```

For full scenario integration details, see `README.md`.

